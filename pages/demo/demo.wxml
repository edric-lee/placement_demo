<!--index.wxml-->
<view class='nav' style='height:{{navH}}px'>
  <view class='nav-title'>
    <text>三年级英语测试成绩</text>
  </view>
</view>

<view class="canvas" style='margin-top:{{navH}}px'>
  <ec-canvas id="mychart-dom-bar" canvas-id="mychart-bar" ec="{{ ec }}"></ec-canvas>
</view>
<view class="box">
  <view class="canvasBox" style='height:{{winH*0.5}}px'>

  </view>
  <view class="reportBox" style='height:{{winH*0.4}}px'>
    <view class="padding flex flex-direction">
      <button class='cu-btn bg-grey lg shadow-blur' >查看报告</button>
    </view>
    <view class="divider">
      <text space="nbsp">- - - - - - - - - - - - - - -   or   - - - - - - - - - - - - - - -</text>
    </view>
    <view class="padding flex flex-direction">
      <button class='cu-btn bg-red margin-tb-sm lg shadow-blur'>推荐课程</button>
    </view>
  </view>

</view>






<!-- <button style="margin-top:10rem;" bindtap="btnclick1">调用模态弹窗(1)</button>
<button style="margin-top:1rem;" bindtap="btnclick2">调用模态弹窗(2)</button> -->
<!--从这里开始复制-->
<!-- <view class="model {{isShowModel?'show':'close'}}" catchtouchmove="preventTouchMove"> -->
<!--弹窗样式可自定义-->
<!-- <view class="model-pack">
    <view class="model-title">{{ModelTitle}}</view>
    <view class="model-content">{{ModelContent}}</view>
    <view class="model-button">
      <view data-modelid="{{ModelId}}" class="model-button-cancel {{isShowConfirm?'close':'show'}}" bindtap="cancel">取消</view>
      <view data-modelid="{{ModelId}}" class="model-button-confirm {{isShowConfirm?'close':'show'}}" bindtap="confirm">确定</view>
      <view data-modelid="{{ModelId}}" class="confirm {{isShowConfirm?'show':'close'}}" bindtap="confirm">确定</view>
    </view>
  </view>
</view>
到这里结束 -->